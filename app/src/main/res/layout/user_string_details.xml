<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/detail_icon_iv"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="24dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/detail_title_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:textSize="10sp"
        app:layout_constraintBottom_toTopOf="@+id/detail_info_tv"
        app:layout_constraintStart_toEndOf="@+id/detail_icon_iv"
        app:layout_constraintTop_toTopOf="@+id/detail_icon_iv"
        tools:text="Nombre y apellidos" />

    <TextView
        android:id="@+id/detail_info_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="@color/activity_main_title"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="@+id/detail_icon_iv"
        app:layout_constraintStart_toStartOf="@+id/detail_title_tv"
        app:layout_constraintTop_toBottomOf="@+id/detail_title_tv"
        tools:text="Javier Carballo Manzano" />
</androidx.constraintlayout.widget.ConstraintLayout>